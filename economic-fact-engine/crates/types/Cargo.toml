[package]
name = "types"
version = "0.1.0"
edition = "2021"

[dependencies]
serde       = { workspace = true }
serde_with  = { version = "3", features = ["macros"] }

[dev-dependencies]
serde_json  = { workspace = true }
